"use client";

/**
 * Kept as a thin re-export so dashboard, schedule and calendar widgets keep a
 * single import site for the workspace brand lens.
 */
export { useBrandScope, useScopedPosts } from "@/hooks/use-posts";
